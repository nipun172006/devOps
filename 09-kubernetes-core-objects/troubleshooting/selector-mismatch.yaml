apiVersion: apps/v1
kind: Deployment
metadata:
  name: selector-error-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expected-label
  template:
    metadata:
      labels:
        app: wrong-label
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
